import {LOGIN} from '../types'
const state={
    pedding:false
}
const mutations={
    [LOGIN](state,action){

    }
}
const getters={
    login(state){

    }
};
export  default {
    state,
    mutations,
    getters
}
